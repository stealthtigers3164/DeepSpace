package frc.robot;

public class arm_test {
    // LimitSwitch switch;

    // public code(int port) {
    //     switch = new LimitSwitch(port);
    // }

    // public void update() {
    //     if (button_down) {
    //         hold = true;
    //     }

    //     if (joysticks not 0) {//Provide a range because these are doubles
    //         hold = false;
    //     }

    //     if (hold) {
    //         if (switch not down) {
    //             encoder_position = getPos();

    //             if (encoder_position < stop_pos) {
    //                 move up the arm
    //             }
    //             else if (encoder_position > stop_pos) {
    //                 move down the arm
    //             }
    //             else {
    //                 stop motor
    //             }
    //         }
    //     }
    // }
}